# Runtime + training dependencies (needed to serve the app and run train.py)
Flask>=3.0
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
gunicorn>=21.0

# To run the training NOTEBOOK (loan_prediction_training.ipynb) instead of
# train.py, also install the extras in requirements-notebook.txt:
#     pip install -r requirements-notebook.txt
